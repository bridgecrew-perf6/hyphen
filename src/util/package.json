{
    "name": "@hyphen/util",
    "version": "0.0.0",
    "description": "Hyphen Shared Utilities",
    "main": "./out/index.js",
    "scripts": {
        "build": "esbuild index.ts --bundle --platform=node --outdir=out --sourcemap=inline"
    }
}